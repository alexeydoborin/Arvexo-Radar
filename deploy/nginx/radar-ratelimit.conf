# Arvexo Radar per-client rate limits (used by sites-available/radar.arvexo.ru).
limit_req_zone $binary_remote_addr zone=radar_api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=radar_auth:10m rate=30r/m;
